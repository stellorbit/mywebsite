name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main

# 権限設定（Workload Identityに必要）
permissions:
  contents: read
  id-token: write
  checks: write

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    
    # 以前エラーが出ていた場所：stepsはここ（jobs > build_and_deploy の下）に置く
    steps:
      # 1. チェックアウト（サブモジュール取得設定を追加）
      - uses: actions/checkout@v4
        with:
          submodules: true  # 👈 これでPapermodが正しくダウンロードされます
          fetch-depth: 0

      # 2. Hugoのセットアップ
      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v3
        with:
          hugo-version: 'latest'
          extended: true

      # 3. Google Cloudへの認証 (Workload Identity)
      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          # 👇 以前取得した長い文字列（projects/...）に書き換えてください
          workload_identity_provider: 'projects/1095497309504/locations/global/workloadIdentityPools/github-pool/providers/github-provider'
          # 👇 あなたのサービスアカウントのアドレスに書き換えてください
          service_account: 'github-action-sa@stellorbit-blog.iam.gserviceaccount.com'

      # 4. ビルド
      - name: Build
        run: hugo --minify

      # 5. デプロイ
      - name: Deploy to Firebase
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: 'github-action-sa@stellorbit-blog.iam.gserviceaccount.com'
          channelId: live
          projectId: stellorbit-blog
